/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Recipe Manager API
 * REST API for creating, reading, updating, and deleting Markdown-based recipes.
 *
 * The API uses JSON throughout, validates all write payloads, and returns
 * [RFC 7807](https://www.rfc-editor.org/rfc/rfc7807) Problem Details on errors.
 *
 * OpenAPI spec version: 0.0.1
 */

export type ListRecipesParams = {
/**
 * Case-insensitive substring match against `title` OR `description`. An empty/absent value means "no filter".
 */
q?: string;
/**
 * Comma-separated tag list. Matches recipes having *any* of the given tags (OR semantics among themselves), AND-ed with `q` if both are present. An empty/absent value means "no filter".
 */
tags?: string[];
/**
 * Sort field and direction as `field,dir`. Allowed fields: `title`, `prepTimeMinutes`, `cookTimeMinutes`, `createdAt`, `updatedAt`. `prepTimeMinutes`/`cookTimeMinutes` always sort `null` values last, regardless of direction. Defaults to `title,asc`.
 */
sort?: string;
/**
 * Zero-based page index. Defaults to `0`.
 * @minimum 0
 */
page?: number;
/**
 * Page size. Defaults to `20`; clamped (not rejected) to a maximum of `100`.
 * @minimum 1
 */
size?: number;
};
